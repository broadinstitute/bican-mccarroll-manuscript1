% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index_snp_start_distance.R
\name{get_index_snp_start_distance}
\alias{get_index_snp_start_distance}
\title{Get start_distance for each index SNP gene-variant pair}
\usage{
get_index_snp_start_distance(
  eqtl_dir,
  region_cell_type_path,
  index_snp_matrix_path,
  output_path = NULL
)
}
\arguments{
\item{eqtl_dir}{Character scalar.  Base directory containing per-cell-type
eQTL result subdirectories (\code{<cell_type>__<region>/}).}

\item{region_cell_type_path}{Character scalar.  Path to a tab-delimited file
with columns \code{cell_type} and \code{region}.}

\item{index_snp_matrix_path}{Character scalar.  Path to the index SNP slope
matrix TSV (output of \code{\link{get_index_snp_slope_matrix_with_impute}}),
must have columns \code{phenotype_id} and \code{variant_id}.}

\item{output_path}{Character scalar or \code{NULL}.  If non-NULL, the result
table is written to this path as a tab-delimited file.}
}
\value{
A \code{data.table} with columns \code{phenotype_id},
  \code{variant_id}, and \code{start_distance}.
}
\description{
Reads \code{start_distance} (variant position minus TSS, in bp) from the
tensorQTL index files for each cell type / region, deduplicates, and joins
to the index SNP list.
}
