% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ctp.R
\name{generate_ctp_ratio_table}
\alias{generate_ctp_ratio_table}
\title{Generate Cell-Type Proportion (CTP) Ratio Table}
\usage{
generate_ctp_ratio_table(
  ctp_df,
  numerator,
  denominator,
  cell_type_col,
  group_cols,
  filters = NULL,
  ratio_name,
  brain_region_col = NULL,
  z_score_threshold = NULL,
  out_file = NULL
)
}
\arguments{
\item{ctp_df}{A data frame containing cell type counts.
Typically generated by `compute_ctp`
Must include columns: `sample_id`, `total_nuclei`, `n_nuclei`}

\item{numerator}{Character vector of cell types to include in the numerator of the ratio.}

\item{denominator}{Character vector of cell types to include in the denominator of the ratio.
If NULL, the denominator will be the total nuclei count for the sample.}

\item{cell_type_col}{Column name in `ctp_df` that contains cell type annotations}

\item{group_cols}{Character vector of columns to group by (e.g., donor ID, brain region).}

\item{filters}{Optional; Character vector of filtering expressions to apply to `ctp_df` before calculating ratios.}

\item{ratio_name}{Name to assign to the calculated ratio}

\item{brain_region_col}{Optional; Column name in `ctp_df` that contains brain region annotations. 
If provided, z-scores of ratios will be computed per brain region.}

\item{z_score_threshold}{Optional; Numeric threshold for flagging outliers based on z-scores of ratios. 
If provided, a new column `outlier` will be added to the output indicating whether each sample is an outlier.}

\item{out_file}{Optional; Output file to save the generated ratio table.}
}
\value{
A data frame with calculated ratios and optional z-scores and outlier flags, grouped by specified columns.
}
\description{
This function calculates ratios between specific cell-type populations across
defined brain regions. It allows for the comparison of a subset of cells
(numerator) against either another subset or the total nuclei count (denominator).
}
