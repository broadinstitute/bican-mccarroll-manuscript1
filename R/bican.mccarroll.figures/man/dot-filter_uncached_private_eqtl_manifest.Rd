% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/private_eqtl_analysis.R
\name{.filter_uncached_private_eqtl_manifest}
\alias{.filter_uncached_private_eqtl_manifest}
\title{Filter a private eQTL manifest down to rows not already cached}
\usage{
.filter_uncached_private_eqtl_manifest(
  manifest_file,
  output_dir,
  force_recompute = FALSE
)
}
\arguments{
\item{manifest_file}{Path to the tab-separated manifest.}

\item{output_dir}{Directory containing (or that will contain) per-analysis
outputs.}

\item{force_recompute}{Logical scalar. If \code{TRUE}, returns the
manifest unfiltered.}
}
\value{
A data.table containing only the manifest rows that still need to
  be run.
}
\description{
For each manifest row, computes the analysis ID and expected output paths
(via \code{bican.mccarroll.eqtl::make_analysis_id()} and
\code{bican.mccarroll.eqtl::make_eqtl_analysis_output_paths()}, the same
helpers \code{run_cell_type_specific_eqtl_tests_from_manifest()} uses
internally to name its outputs) and drops rows whose
\code{.summary.txt} and \code{.linear_model_test.txt} already exist under
\code{output_dir}. This is the sole place caching decisions are made for
the private eQTL pipeline; \code{bican.mccarroll.eqtl} has no cache/skip
logic of its own.
}
