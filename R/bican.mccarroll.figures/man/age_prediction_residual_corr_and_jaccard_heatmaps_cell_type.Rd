% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_pred_figures.R
\name{age_prediction_residual_corr_and_jaccard_heatmaps_cell_type}
\alias{age_prediction_residual_corr_and_jaccard_heatmaps_cell_type}
\title{Manuscript figure: residual correlation heatmap + Jaccard overlap heatmap
across cell types
Create a single SVG with two heatmaps on the same row:
(1) Jaccard overlap of aging programs (ordered to match the residual correlation heatmap)
(2) Residual correlation heatmap (clustered; provides the ordering)}
\usage{
age_prediction_residual_corr_and_jaccard_heatmaps_cell_type(
  cell_type = "microglia",
  metacell_dir = NULL,
  data_name = "donor_rxn_DGEList",
  age_de_results_dir = NULL,
  contig_yaml_file = NULL,
  reduced_gtf_file = NULL,
  n_cores = 14,
  data_cache_dir = NULL,
  outDir = NULL
)
}
\arguments{
\item{cell_type}{Character scalar cell type name (e.g., "astrocyte").}

\item{metacell_dir}{See `get_age_prediction_results()`.}

\item{data_name}{See `get_age_prediction_results()`.}

\item{age_de_results_dir}{See `get_age_prediction_results()`.}

\item{contig_yaml_file}{See `get_age_prediction_results()`.}

\item{reduced_gtf_file}{See `get_age_prediction_results()`.}

\item{n_cores}{See `get_age_prediction_results()`.}

\item{data_cache_dir}{Cache root directory used by `get_age_prediction_results()`.}

\item{outDir}{Output directory for the SVG.}
}
\value{
Invisibly returns a list with components `corr_out`, `jaccard_out`, and `out_svg`.
}
\description{
Cell types are restricted to those listed in `cellTypeListFile` (one cell type per line,
no header). Data are fetched via `get_age_prediction_results()` and cached as usual.
}
