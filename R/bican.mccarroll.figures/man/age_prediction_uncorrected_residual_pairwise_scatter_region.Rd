% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_pred_figures.R
\name{age_prediction_uncorrected_residual_pairwise_scatter_region}
\alias{age_prediction_uncorrected_residual_pairwise_scatter_region}
\title{Manuscript figure: pairwise uncorrected residual scatterplots within a region}
\usage{
age_prediction_uncorrected_residual_pairwise_scatter_region(
  cell_type_list = c("astrocyte", "OPC", "microglia", "SPN_D1"),
  region = "CaH",
  metacell_dir = NULL,
  data_name = "donor_rxn_DGEList",
  age_de_results_dir = NULL,
  use_age_de_results = TRUE,
  contig_yaml_file = NULL,
  reduced_gtf_file = NULL,
  n_cores = 14,
  data_cache_dir = NULL,
  outDir = NULL,
  optimize_alpha = FALSE,
  alpha_fixed = 0,
  ncol = 3
)
}
\arguments{
\item{cell_type_list}{Character vector of cell types to include.}

\item{region}{Character scalar region name (e.g., "CaH").}

\item{metacell_dir}{See `get_age_prediction_results()`.}

\item{data_name}{See `get_age_prediction_results()`.}

\item{age_de_results_dir}{See `get_age_prediction_results()`.}

\item{use_age_de_results}{Logical; if \code{TRUE}, resolve and use
\code{age_de_results_dir} when building the model. If \code{FALSE},
age differential expression results are not used.}

\item{contig_yaml_file}{See `get_age_prediction_results()`.}

\item{reduced_gtf_file}{See `get_age_prediction_results()`.}

\item{n_cores}{See `get_age_prediction_results()`.}

\item{data_cache_dir}{Cache root directory used by `get_age_prediction_results()`.}

\item{outDir}{Output directory for the SVG.}

\item{optimize_alpha}{Logical; if \code{TRUE}, the elastic net mixing
parameter alpha is optimized rather than fixed.}

\item{alpha_fixed}{Numeric elastic net mixing parameter used when
\code{optimize_alpha} is \code{FALSE}.}

\item{ncol}{Number of columns in the panel grid.}
}
\value{
Invisibly returns the assembled ggplot object.
}
\description{
Generate a single SVG containing all pairwise scatterplots of corrected donor
residuals across a specified list of cell types within a fixed region.
Points correspond to donors present in both groups and are colored by donor age.
}
