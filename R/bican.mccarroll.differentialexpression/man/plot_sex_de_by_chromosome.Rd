% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DE_summary_barplot.R
\name{plot_sex_de_by_chromosome}
\alias{plot_sex_de_by_chromosome}
\title{Plot significant sex effects by chromosome group}
\usage{
plot_sex_de_by_chromosome(
  in_dir,
  file_pattern,
  contig_yaml_file,
  reduced_gtf_file,
  cellTypeListFile = NULL,
  alpha = 0.05,
  pdf_output_file = NULL,
  svg_barplot_file = NULL,
  svg_density_dir = NULL,
  svg_density_prefix = ""
)
}
\arguments{
\item{in_dir}{Directory containing differential-expression result files.}

\item{file_pattern}{Pattern used by \code{parse_de_inputs()}.}

\item{contig_yaml_file}{Path to the contig classification YAML file.}

\item{reduced_gtf_file}{Path to the reduced GTF-like annotation file.}

\item{cellTypeListFile}{Optional file containing cell types to include.}

\item{alpha}{Adjusted P-value threshold.}

\item{pdf_output_file}{Optional path for the output multi-page PDF. When
\code{NULL}, no PDF is written.}

\item{svg_barplot_file}{Optional path for writing the summary count plot
to an SVG file. When \code{NULL}, no barplot SVG is written.}

\item{svg_density_dir}{Optional directory for writing one signed-effect
density SVG per region. When \code{NULL}, no density SVGs are
written.}

\item{svg_density_prefix}{Filename prefix used for each density SVG
written to \code{svg_density_dir}.}
}
\value{
Invisibly returns the significant results, count data, and plots.
}
\description{
Parses differential-expression results, annotates genes as autosomal or
X/Y, and writes count and signed-effect density plots. Three independent
output paths are supported, each defaulting to \code{NULL} (skipped):
a multi-page \code{pdf_output_file} (the summary count plot followed by
one density plot per region, as before), a single \code{svg_barplot_file}
holding just the summary count plot, and \code{svg_density_dir}, a
directory that receives one signed-effect density SVG per region (all
cell types faceted within a single plot, as in the PDF pages), named
\code{<svg_density_prefix><region>.svg}.
}
