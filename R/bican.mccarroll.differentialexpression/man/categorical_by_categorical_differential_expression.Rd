% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_expression.R
\name{categorical_by_categorical_differential_expression}
\alias{categorical_by_categorical_differential_expression}
\title{Categorical x categorical DE via a combined factor (named by contrast_defs)}
\usage{
categorical_by_categorical_differential_expression(
  dge_cell,
  fixedVars,
  randVars,
  contrast_defs,
  factor_var = "imputed_sex",
  interaction_var = "region",
  verbose = TRUE,
  n_cores = parallel::detectCores() - 2
)
}
\arguments{
\item{dge_cell}{DGEList-like with counts and \code{samples}.}

\item{fixedVars}{Character vector of extra fixed effects to include. Do NOT include
\code{factor_var}, \code{interaction_var}, or their interaction; this function
constructs the combined factor internally.}

\item{randVars}{Character vector of random-effect grouping variables (random intercepts).}

\item{contrast_defs}{data.frame with columns \code{contrast_name}, \code{variable},
\code{reference_level}, \code{comparison_level}. Rows for \code{variable == factor_var}
define which pairs to test, and provide human-friendly names.}

\item{factor_var}{Character scalar. Primary categorical variable (e.g. "imputed_sex").}

\item{interaction_var}{Character scalar. Stratifying categorical variable (e.g. "region").}

\item{verbose}{Logical.}

\item{n_cores}{Integer workers for \code{BiocParallel::MulticoreParam()}.}
}
\value{
Named list of \code{data.frame}s. Each is a \code{variancePartition::topTable} for a contrast
  \code{<contrast_name>_<region>} testing mean(comparison, region) - mean(reference, region) = 0.
}
\description{
Builds a combined factor \code{combo = interaction(factor_var, interaction_var)} so each
coefficient is one cell of the grid. Fits \code{~ 0 + combo + other_fixed + (1|rand...)}
with voom+dream, constructs within-\code{interaction_var} contrasts for the pairs listed
in \code{contrast_defs} where \code{variable == factor_var}, and returns a named list of
\code{topTable} results. Names are \code{<contrast_name>_<region>}, e.g. \code{female_vs_male_CaH}.
}
