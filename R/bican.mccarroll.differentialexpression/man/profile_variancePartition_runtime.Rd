% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variancePartition.R
\name{profile_variancePartition_runtime}
\alias{profile_variancePartition_runtime}
\title{Profile Runtime of Variance Partitioning by Gene Batches}
\usage{
profile_variancePartition_runtime(
  exprObj,
  formula,
  data,
  batch_size = 1000,
  BPPARAM = BiocParallel::SerialParam(),
  verbose = TRUE
)
}
\arguments{
\item{exprObj}{A gene expression object such as a matrix, `DGEList`, or any format
compatible with `variancePartition::fitExtractVarPartModel()`. Rows represent genes.}

\item{formula}{A model formula specifying the variance partitioning design, typically
involving covariates in `data` (e.g., `~ (1|Individual) + Age + Sex`).}

\item{data}{A data frame with metadata corresponding to the columns (samples) in `exprObj`.
Must contain all variables referenced in `formula`.}

\item{batch_size}{Integer. Number of genes to include in each batch (default is 1000).}

\item{BPPARAM}{A BiocParallel parameter object used to control parallel processing.
Defaults to `BiocParallel::SerialParam()` (serial execution).}

\item{verbose}{Logical. If `TRUE` (default), print progress messages and per-batch runtimes.}
}
\value{
A variancePartition object with variance estimates for all genes,
  as returned by `variancePartition::fitExtractVarPartModel()`, combined across batches.
}
\description{
Runs `variancePartition::fitExtractVarPartModel()` in batches to profile runtime
across a large gene expression dataset, reporting per-batch and total runtimes.
Designed for benchmarking or runtime monitoring on large datasets.
}
