% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_expression.R
\name{differential_expression_group_interactions}
\alias{differential_expression_group_interactions}
\title{Differential expression for between-group differences in a continuous effect}
\usage{
differential_expression_group_interactions(
  data_dir,
  data_name,
  randVars,
  fixedVars,
  group_interaction_file,
  tested_var = "age",
  outPDF = NULL,
  result_dir,
  n_cores = parallel::detectCores() - 2,
  cpm_cutoff = 1,
  fraction_samples = 0.1
)
}
\arguments{
\item{data_dir}{Directory containing the serialized DGEList input data.}

\item{data_name}{Name of the DGEList dataset (without file extension).}

\item{randVars}{Character vector of random effect variables to include in
the model.}

\item{fixedVars}{Character vector of fixed effect variables to include in
the model.}

\item{group_interaction_file}{Path to a tab-delimited file defining the group
comparisons. The file must contain the columns `baseline_celltype`,
`baseline_region`, `comparison_celltype`, and `comparison_region`.}

\item{tested_var}{Name of the continuous variable whose effect is compared
between groups (default `"age"`).}

\item{outPDF}{Optional path to a PDF file for volcano plots. If `NULL`,
plots are not written.}

\item{result_dir}{Directory where differential expression results will be
written.}

\item{n_cores}{Number of cores used for model fitting.}

\item{cpm_cutoff}{Minimum CPM threshold used during gene filtering.}

\item{fraction_samples}{Fraction of samples required to exceed the CPM
threshold for a gene to be retained.}
}
\value{
Invisibly returns `NULL`. Differential expression tables are written
  to `result_dir`.
}
\description{
This entry point extends the standard differential expression workflow by
testing whether the effect of a continuous variable (for example, age)
differs between two groups. Groups are defined by `(cell_type, region)` and
are provided row-wise in `group_interaction_file` as a baseline group and a
comparison group.
}
\details{
For each row in `group_interaction_file`, the function fits an interaction
model and writes only the group-difference (interaction) results for the
tested variable.
}
