% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_external_predictions.R
\name{load_external_dge_for_age_prediction}
\alias{load_external_dge_for_age_prediction}
\title{Load an external DGEList for age prediction, rescaling age to decades}
\usage{
load_external_dge_for_age_prediction(
  data_dir,
  data_prefix = "donor_rxn_DGEList",
  age_col = "age",
  age_divisor = 10
)
}
\arguments{
\item{data_dir}{Directory containing \code{<data_prefix>_counts.tsv.gz} and
\code{<data_prefix>_samples.tsv.gz}, as written by \code{saveDGEList()}.}

\item{data_prefix}{Filename prefix passed to \code{loadDGEList()}.
Default \code{"donor_rxn_DGEList"}.}

\item{age_col}{Column in \code{dge$samples} holding chronological age.
Default \code{"age"}.}

\item{age_divisor}{Value to divide \code{age_col} by to convert to decades
(e.g. \code{10} if the external data stores age in years). Set to
\code{1} if the data is already in decades.}
}
\value{
A \code{DGEList} with raw counts and \code{age_col} rescaled.
}
\description{
Wraps \code{loadDGEList()} and rescales \code{dge$samples[[age_col]]} so
that it is in the same units (decades) the age prediction models in this
package are trained on. \code{\link{predict_age_from_dge}} expects
\code{dge$samples$donor} and \code{dge$samples$age} specifically, so
\code{age_col} must already be (or be renamed to) \code{"age"}.
}
\keyword{internal}
