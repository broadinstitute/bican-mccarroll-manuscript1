% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DE_summary_barplot.R
\name{compute_de_result_counts}
\alias{compute_de_result_counts}
\title{Compute per-cell-type differential expression effect counts}
\usage{
compute_de_result_counts(
  in_dir,
  file_pattern,
  cellTypeListFile = NULL,
  fdr_cutoff = 0.05
)
}
\arguments{
\item{in_dir}{Directory containing the differential expression result files.}

\item{file_pattern}{Pattern used to identify differential expression result
files.}

\item{cellTypeListFile}{Optional path to a file specifying the cell types to
include.}

\item{fdr_cutoff}{Adjusted P-value threshold used to call a gene
significant. Defaults to 0.05.}
}
\value{
A data.table with columns \code{cell_type}, \code{interaction},
  \code{contrast}, \code{n_up}, and \code{n_down} (one row per cell type /
  interaction combination).
}
\description{
Parses differential expression result files and counts genes with
positive and negative log fold changes passing an adjusted P-value
threshold of 0.05, for each cell type / interaction combination. This is
the data-gathering half of \code{barplot_de_results()}, split out so
callers can cache the counts and skip re-parsing the raw DE files on
subsequent plot renders.
}
