% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createDGEList.R
\name{getAnnotations}
\alias{getAnnotations}
\title{Aggregate Cell-Level Metadata to Donor-Level Summaries}
\usage{
getAnnotations(
  metricsDF,
  aggregationFeatures = c("donor_external_id"),
  additionalColumns = c("age", "imputed_sex"),
  addCounts = FALSE
)
}
\arguments{
\item{metricsDF}{A data frame with per-cell metadata, including donor ID and columns to aggregate.}

\item{aggregationFeatures}{A character vector of column names to group by (e.g., \code{"donor_external_id"}).}

\item{additionalColumns}{A character vector of column names to summarize for each group.}

\item{addCounts}{Logical; if \code{TRUE}, adds a column \code{n_cells} with the number of cells per group.}
}
\value{
A data frame with one row per donor and columns for the aggregated metadata.
}
\description{
Aggregates one or more columns from a cell-level metadata data frame into donor-level summaries.
Numeric columns are averaged, and categorical columns must be consistent within each group.
}
\keyword{internal}
