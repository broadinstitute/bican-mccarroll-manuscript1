% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DE_summary_barplot.R
\name{barplot_de_results_from_counts}
\alias{barplot_de_results_from_counts}
\title{Plot differential expression result counts}
\usage{
barplot_de_results_from_counts(
  de_counts,
  pdf_output_file = NULL,
  svg_output_file = NULL,
  width = NULL,
  height = NULL
)
}
\arguments{
\item{de_counts}{A data.table produced by \code{compute_de_result_counts()},
with columns \code{cell_type}, \code{interaction}, \code{contrast},
\code{n_up}, and \code{n_down}.}

\item{pdf_output_file}{Optional path for writing the plot to a PDF file.
When `NULL`, no PDF is written.}

\item{svg_output_file}{Optional path for writing the plot to an SVG file.
When `NULL`, no SVG is written.}

\item{width}{Optional plot width (inches) passed to `ggplot2::ggsave()`.}

\item{height}{Optional plot height (inches) passed to `ggplot2::ggsave()`.}
}
\value{
A `ggplot` object (or a `cowplot` grob when the data span more than
  one region/interaction).
}
\description{
Creates faceted bar plots showing the number of genes with positive and
negative log fold changes, from a counts table produced by
\code{compute_de_result_counts()}.
}
