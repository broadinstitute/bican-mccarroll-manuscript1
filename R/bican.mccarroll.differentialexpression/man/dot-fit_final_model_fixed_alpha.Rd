% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_model_fit_core.R
\name{.fit_final_model_fixed_alpha}
\alias{.fit_final_model_fixed_alpha}
\title{Fit final elastic net model with fixed alpha on all provided data}
\usage{
.fit_final_model_fixed_alpha(X_all, y, alpha_fixed)
}
\arguments{
\item{X_all}{Numeric matrix (samples x genes), unscaled logCPM values.}

\item{y}{Numeric response vector (age), length = nrow(X_all).}

\item{alpha_fixed}{Numeric alpha in [0, 1].}
}
\value{
A list with:
\itemize{
  \item \code{final_model}: data.frame with columns \code{feature}, \code{coef},
    \code{mean_train}, \code{sd_train}, \code{intercept}.
  \item \code{final_alpha}: the alpha used (same as \code{alpha_fixed}).
  \item \code{final_lambda}: selected \code{lambda.min}.
}
}
\description{
Internal helper to fit a final model on all samples in \code{X_all} given a
fixed alpha. Uses \code{glmnet::cv.glmnet()} to select \code{lambda.min} and
returns a model data.frame in the same format expected by
\code{predict_age_from_dge()}.
}
\keyword{internal}
