% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_model_fit_core.R
\name{.select_alpha_lambda}
\alias{.select_alpha_lambda}
\title{Select (alpha, lambda) for elastic net by grid search over alpha}
\usage{
.select_alpha_lambda(X, y, alpha_grid, type.measure = "mae", seed = NULL)
}
\arguments{
\item{X}{Numeric matrix (samples x features), typically already scaled.}

\item{y}{Numeric response vector (length = nrow(X)).}

\item{alpha_grid}{Numeric vector of candidate alpha values in [0, 1].}

\item{type.measure}{Character; passed to \code{glmnet::cv.glmnet()}.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A list with:
\itemize{
  \item \code{alpha}: selected alpha.
  \item \code{lambda_min}: selected lambda (cv lambda.min for selected alpha).
  \item \code{cvmin}: best (minimum) achieved cross-validation loss.
}
}
\description{
Internal helper that performs a grid search over \code{alpha_grid} and uses
\code{glmnet::cv.glmnet()} to select \code{lambda.min} for each alpha. Returns
the alpha whose cross-validated loss is minimal, along with its lambda.
}
\keyword{internal}
