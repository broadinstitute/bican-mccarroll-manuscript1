% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sex_chromosome_leakage.R
\name{sex_chromosome_leakage_from_files}
\alias{sex_chromosome_leakage_from_files}
\title{Compute sex-chromosome expression leakage ratios (file interface)}
\usage{
sex_chromosome_leakage_from_files(
  data_dir,
  data_name,
  cellTypeListFile = NULL,
  outFile = NULL,
  outFileDonorLevel = NULL,
  outSVG = NULL,
  xist_gene = "XIST",
  y_genes = c("DDX3Y", "RPS4Y1", "USP9Y")
)
}
\arguments{
\item{data_dir}{Directory containing the serialized DGEList (passed to
\code{\link{loadDGEList}} as \code{dir}).}

\item{data_name}{Prefix used by \code{\link{loadDGEList}} to locate the
\code{*_counts.tsv.gz} / \code{*_samples.tsv.gz} files (passed as
\code{prefix}).}

\item{cellTypeListFile}{Optional file path containing a list of cell
types to include (passed to
\code{\link{filter_dgelist_by_celltype_list}}). If \code{NULL}, uses
all cell types present.}

\item{outFile}{Optional tab-delimited output path for the village-level
leakage results table. If \code{NULL}, no file is written.}

\item{outFileDonorLevel}{Optional tab-delimited output path for the
donor-level leakage results table (see
\code{\link{compute_sex_chromosome_leakage_donor_level}}). If
\code{NULL}, no file is written.}

\item{outSVG}{Optional SVG output path for the leakage summary boxplot. If
\code{NULL}, no file is written.}

\item{xist_gene}{Character scalar naming the female-specific marker gene.
Default \code{"XIST"}.}

\item{y_genes}{Character vector naming the male-specific Y-chromosome
genes to sum. Default \code{c("DDX3Y", "RPS4Y1", "USP9Y")}.}
}
\value{
Invisibly returns a list with elements:
  \itemize{
    \item \code{leakage}: the village-level data frame produced by
      \code{\link{compute_sex_chromosome_leakage}}.
    \item \code{donor_leakage}: the donor-level data frame produced by
      \code{\link{compute_sex_chromosome_leakage_donor_level}}.
    \item \code{plot}: the \code{ggplot2} object produced by
      \code{\link{plot_sex_chromosome_leakage_boxplot}}.
  }
}
\description{
Wrapper around \code{\link{compute_sex_chromosome_leakage}} that loads a
\code{DGEList} from disk (via \code{\link{loadDGEList}}), optionally
restricts to a set of cell types (via
\code{\link{filter_dgelist_by_celltype_list}}), computes leakage ratios,
and optionally writes the results table and a summary plot to disk.
}
