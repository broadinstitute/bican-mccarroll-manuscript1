% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_external_predictions.R
\name{pivot_age_prediction_comparison}
\alias{pivot_age_prediction_comparison}
\title{Pivot a long-format age-prediction comparison table to a wide display table}
\usage{
pivot_age_prediction_comparison(
  comparison_df,
  metric = "mean_abs_error",
  age_scale = 1,
  variant_order = NULL,
  sort_by = NULL,
  add_mean_row = TRUE
)
}
\arguments{
\item{comparison_df}{As returned by \code{\link{compare_age_predictions_to_reference}}.}

\item{metric}{Column of \code{comparison_df} to pivot into wide format.
Default \code{"mean_abs_error"}.}

\item{age_scale}{Multiplier applied to \code{metric} for display (e.g.
\code{10} to convert decades to years). Default \code{1} (no conversion).}

\item{variant_order}{Optional character vector giving the column order for
the pivoted variants. Default is the order variants first appear in
\code{comparison_df}.}

\item{sort_by}{Variant name to sort rows by (ascending). Default is the
first variant in \code{variant_order}.}

\item{add_mean_row}{Logical; append a row with the column-wise mean.
Default \code{TRUE}.}
}
\value{
A wide data.frame: one row per cell type, one column per variant.
}
\description{
Pivot a long-format age-prediction comparison table to a wide display table
}
\keyword{internal}
